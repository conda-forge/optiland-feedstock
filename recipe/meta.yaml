{% set name = "optiland" %}
{% set version = "0.5.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/optiland-{{ version }}.tar.gz
  sha256: 9de06f315c4e36913ad428964c8925d9bc72c73f0ec17fd60655e5df84f40d12

build:
  entry_points:
    - optiland = optiland_gui.run_gui:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - pip
  run:
    - python >={{ python_min }}
    - numpy
    - scipy
    - pandas
    - pyyaml
    - matplotlib-base
    - vtk
    - tabulate
    - numba
    - requests
    - seaborn
    - typing-extensions >=4.13.2
    - PySide6
    - qtconsole

test:
  imports:
    - optiland
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/HarrisonKramer/optiland
  summary: Open Source Optical Design in Python
  license: MIT
  license_file: LICENSE
  doc_url: https://optiland.readthedocs.io/
  dev_url: https://github.com/HarrisonKramer/optiland

extra:
  recipe-maintainers:
    - crnh
    - HarrisonKramer
