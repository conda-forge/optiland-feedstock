{% set name = "optiland" %}
{% set version = "0.6.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/optiland-{{ version }}.tar.gz
  sha256: 3fdbc3f0517d26d4072966dc393ba2c523117caee745a73b13f4c26b529a0e89

build:
  entry_points:
    - optiland = optiland_gui.run_gui:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - pip
  run:
    - python >={{ python_min }}
    - numpy
    - scipy
    - pandas
    - pyyaml
    - matplotlib-base
    - vtk
    - tabulate
    - numba >=0.60.0
    - requests
    - seaborn
    - typing-extensions >=4.1.0
    - PySide6
    - qtconsole

test:
  imports:
    - optiland
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/HarrisonKramer/optiland
  summary: Open Source Optical Design in Python
  license: MIT
  license_file: LICENSE
  doc_url: https://optiland.readthedocs.io/
  dev_url: https://github.com/HarrisonKramer/optiland

extra:
  recipe-maintainers:
    - crnh
    - HarrisonKramer
